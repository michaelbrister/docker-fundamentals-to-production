# PROVENANCE (Lab 19)

REPLACE_ME — Fill this out. Validators will fail if placeholders remain.

## Release candidate
- RC_VERSION:
- RC tag: dzth/mini-platform-api:${RC_VERSION}

## Immutable artifact identifier
- PROMOTED_IMAGE_REF (image ID): sha256:...

## Evidence
Paste the output of:
`docker image inspect dzth/mini-platform-api:${RC_VERSION} --format '{{.Id}}'`

## Promotion proof
Describe how you verified prod-like is running the same image ID.
