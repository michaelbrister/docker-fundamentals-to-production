# Lab 19 config (non-secret)
RC_VERSION=0.6.0-rc.1

# After you build in dev, set this to the immutable image ID:
# Example: PROMOTED_IMAGE_REF=sha256:abc123...
PROMOTED_IMAGE_REF=REPLACE_ME

DB_HOST=db
DB_PORT=5432
DB_NAME=appdb
DB_USER=app
