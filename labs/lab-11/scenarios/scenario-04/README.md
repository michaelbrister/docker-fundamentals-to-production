# Scenario 4 — Override leak (environment shape)

## Intentional break
A dev override file changes the API execution model (bind mount + go run).

## What success looks like
- You can explain why override files can change behavior
- You can run the system in a prod-like way by controlling compose file selection
