# Scenario 2 — Localhost regression (networking)

## Intentional break
`DB_HOST` is set to `localhost` inside the API container.

## What success looks like
- You can explain why localhost is wrong here
- `/readyz` becomes ready after fix
