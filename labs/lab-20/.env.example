# Lab 20 (Final Exam) — non-secret config
RC_VERSION=0.7.0-rc.1

# After you build the known-good image ONCE, set both refs to the immutable image ID:
# Example: sha256:abc123...
KNOWN_GOOD_IMAGE_REF=REPLACE_ME
PROMOTED_IMAGE_REF=REPLACE_ME

DB_HOST=db
DB_PORT=5432
DB_NAME=appdb
DB_USER=app
