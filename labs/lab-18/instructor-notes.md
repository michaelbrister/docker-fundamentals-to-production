# Lab 18 — Instructor Notes
Teach immutable artifacts + calm rollback:
- build good + bad images once
- deploy bad, observe evidence
- rollback by switching ACTIVE_VERSION (no rebuild)
Fail if prod compose contains `build:` or learner skips INCIDENT.md.
