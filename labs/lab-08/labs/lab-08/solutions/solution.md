# Lab 08 — Solution (reference)

- Start stack: `docker compose up -d`
- Verify `/healthz` then `/readyz`
- GET and POST `/notes`
- Restart api: `docker compose restart api`
- Cleanup: `docker compose down`
