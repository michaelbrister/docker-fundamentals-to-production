# Lab 05 — Hints

## If services can’t reach each other
- Use service names, not localhost

## If ports don’t work
- Check `compose.yaml` port mappings

## If containers keep running
- Use `docker compose down`
