# Secrets overlay (kept explicit for teaching; base already wires secrets)
services:
  api:
    environment:
      DB_PASSWORD: ""
      DB_PASSWORD_FILE: "/run/secrets/db_password"
  db:
    environment:
      POSTGRES_PASSWORD: ""
      POSTGRES_PASSWORD_FILE: "/run/secrets/db_password"
