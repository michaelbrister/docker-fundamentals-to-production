# Optional overlay — kept for continuity with prior labs.
# This file is intentionally small: it shows you can layer secrets/config per environment.
services:
  api:
    environment:
      LOG_LEVEL: "debug"
