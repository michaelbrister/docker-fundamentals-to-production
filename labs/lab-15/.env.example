# Non-secret configuration only
APP_ENV=dev
LOG_LEVEL=info

# DB connection (non-secret)
DB_HOST=db
DB_PORT=5432
DB_NAME=appdb
DB_USER=app

# Optional: inject latency to demonstrate metrics (ms)
SIMULATE_LATENCY_MS=0
